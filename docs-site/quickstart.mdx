---
title: "Quickstart"
description: "Start OpenNivara and verify the desktop app."
---

Use the desktop app for real validation. Browser preview is only for fast React iteration.

```powershell
cd desktop
bun install
bun run tauri:dev
```

Open Memory from the app sidebar, run a context audit, and verify normal chat does not include memory or location.
