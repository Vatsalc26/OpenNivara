---
title: "Context Compiler"
description: "Deterministic audited prompt assembly."
---

The Context Compiler decides what the model receives in one fresh request.

It routes by intent, applies privacy gates, checks runtime/location relevance, retrieves memory, trims by token budget, and records an audit.
