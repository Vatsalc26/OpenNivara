---
title: "Troubleshooting"
description: "Common desktop checks."
---

Use these checks first:

- Run `bun run typecheck` from `desktop`.
- Run `bun run check` from `desktop`.
- Run `cargo check` from `desktop/src-tauri`.
- Use Tauri E2E for Rust-backed desktop flows.
