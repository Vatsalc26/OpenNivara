# Store Themes Only

The Store is themes-only. Themes may change visual appearance but never alter prompt assembly, memory, tools, settings, preferences, contexts, or assistant behavior.

Settings controls assistant behavior. Any prompt contribution must come from Settings, runtime context, memory compiler decisions, workspace context, or explicit user action.

Tests should fail if Store content changes prompt behavior.
