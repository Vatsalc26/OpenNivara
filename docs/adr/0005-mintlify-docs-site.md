# ADR 0005: Mintlify Docs Site

Status: accepted.

OpenNivara has an internal docs tree in `docs/` and a public-facing Mintlify docs site in `docs-site/`.

Mintlify pages summarize stable product behavior. Internal implementation notes, stale docs, private decisions, and development scratch work stay in `docs/`.
